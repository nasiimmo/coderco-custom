name: CoderCo custom action
description: Greet someone

inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'Hello'

outputs:
  time:
    description: 'Time greeted'

runs:
  using: 'node18'
  main: 'dist/index.js'